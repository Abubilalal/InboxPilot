# ── Required ────────────────────────────────────────────────────
APP_SECRET=        # JWT signing key. Generate with: openssl rand -base64 32
DATABASE_URL=      # MySQL connection string from Aiven
                   # e.g. mysql://avnadmin:PASSWORD@HOST.aivencloud.com:PORT/defaultdb
